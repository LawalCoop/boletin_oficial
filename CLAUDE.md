# entrelinias

## Dev logs

Next.js dev server logs are written to `dev.log` (via `npm run dev`). Read this file to diagnose runtime errors, API issues, or build warnings without needing terminal access.

## Run locally

```bash
npm install
npm run dev
```

Portal at http://localhost:3000. The repo includes pre-processed articles — no API keys needed to browse.
